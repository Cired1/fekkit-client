import styles from "./PostDetails.module.css"

const PostDetails = () => {
    return (
        <div className={styles.container}>
            details
        </div>
    )
}

export default PostDetails
